# Sample environment file. Copy to .env and fill in your values:
#   cp .env.example .env
# .env is git-ignored; this file (.env.example) is safe to commit.

# Firecrawl API key (https://firecrawl.dev) — used by the --mcp tool path.
FIRECRAWL_API_KEY=
